spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASS}
spring.jpa.database-platform=${URL_BASE}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${STATUS_BASE}
jwt.secret=${JWT_SECRET_KEY}
jwt.expiration=${EXPIRATION_TIMER}
google.places.api.key=${GOOGLE_PLACES_API_KEY}
google.places.api.url=${GOOGLE_PLACES_API_URL}

cloudinary.cloud_name=${CLOUD_NAME}
cloudinary.api_key=${KEY_NAME}
cloudinary.api_secret=${API_KEY_SECRET}

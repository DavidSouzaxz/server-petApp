name: Keep Alive Render

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Hit the stealth endpoint
        run: >-
          curl -s -H "X-Health-Key: ${{ secrets.HEALTH_KEY }}" ${{ secrets.RENDER_URL }}
